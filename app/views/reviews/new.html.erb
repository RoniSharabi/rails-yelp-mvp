<h1>Add new review <%= @restaurant.name %></h1>

<%= form_for [@restaurant, @review]  do |f|%>
  <%= f.text_field :content %>
  <%= f.integer_field :rating %>
  <%= f.submit %>
<% end %>

<%= link_to 'Back', reviews_path %>
